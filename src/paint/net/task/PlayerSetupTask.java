package paint.net.task;

import java.util.concurrent.Callable;

public class PlayerSetupTask implements Callable<Boolean> {
    @Override
    public Boolean call() throws Exception {
        return null;
    }
}
